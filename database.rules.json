{
  "rules": {
    "presence": {
      "$roomId": {
        ".read": "auth != null",
        "$uid": {
          "$connectionId": {
            ".write": "auth != null && auth.uid === $uid"
          }
        }
      }
    }
  }
}
